import com.sun.java.swing.plaf.windows.WindowsTextAreaUI;

public class Calculadora {

   // make return statement in one line

   int sumar(Integer number, Integer number1 ){
      return number + number1;

   }
   int restar(Integer number,Integer number1){

      return number - number1;
   }
   int multiplicar(Integer number,Integer number1){

      return number * number1;
   }
   int dividir(Integer number, Integer number1){

      return number / number1;

   }
}
